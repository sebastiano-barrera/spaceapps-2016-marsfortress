function loadSprite (obj) {
    // id 
    // size.x
    // size.y
    // start.x
    // start.y
    var id = obj.id;
    var sizeX = obj.size.x;
    var sizeY = obj.size.y;
    var startX = obj.start.x;
    var startY = obj.start.y;

}
